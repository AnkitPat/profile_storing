# App is for storing general information of users in storage, it include AsyncStorage for storing information. It also includes image picker to pick image from gallery.

It contains react navigation also.
